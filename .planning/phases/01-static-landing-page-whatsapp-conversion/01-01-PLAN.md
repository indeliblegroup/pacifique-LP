---
phase: 01-static-landing-page-whatsapp-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/app/not-found.tsx
  - src/components/ui/section-wrapper.tsx
  - src/components/navbar.tsx
  - src/components/whatsapp-fab.tsx
  - src/lib/constants.ts
  - postcss.config.mjs
  - package.json
  - tsconfig.json
autonomous: true
requirements:
  - HERO-02
  - HERO-03
  - CONV-01
  - RESP-01
  - RESP-04
  - COMP-03

must_haves:
  truths:
    - "Next.js dev server starts with `pnpm dev` and renders a page at localhost:3000"
    - "Tailwind design tokens from CLAUDE.md palette are available as utility classes (e.g., bg-primary, text-text-body, bg-bg-lavender)"
    - "Sticky navbar renders with desktop horizontal links and mobile hamburger toggle"
    - "Smooth scroll works when clicking nav anchor links"
    - "WhatsApp floating button is visible and links to wa.me with pre-filled message"
    - "Page has no horizontal scroll at 375px viewport width"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with lang=pt-BR, fonts, scroll-smooth, metadata"
      contains: "lang=\"pt-BR\""
    - path: "src/app/globals.css"
      provides: "Tailwind v4 import + @theme design tokens"
      contains: "@theme"
    - path: "src/components/navbar.tsx"
      provides: "Sticky navigation with hamburger menu and scroll spy"
      contains: "use client"
    - path: "src/components/whatsapp-fab.tsx"
      provides: "Floating WhatsApp button with wa.me deep link"
      contains: "wa.me"
    - path: "src/components/ui/section-wrapper.tsx"
      provides: "Reusable section wrapper with alternating bg-white/bg-lavender"
      exports: ["SectionWrapper"]
    - path: "src/lib/constants.ts"
      provides: "Shared data constants (nav items, WhatsApp number, section IDs, team data, nuclei data, FAQ data)"
      exports: ["NAV_ITEMS", "WHATSAPP_NUMBER", "WHATSAPP_MESSAGE"]
  key_links:
    - from: "src/components/navbar.tsx"
      to: "section IDs"
      via: "anchor hrefs matching section id attributes"
      pattern: "href.*#"
    - from: "src/components/whatsapp-fab.tsx"
      to: "wa.me external link"
      via: "anchor tag with WHATSAPP_NUMBER from constants"
      pattern: "wa\\.me.*WHATSAPP_NUMBER"
    - from: "src/app/globals.css"
      to: "Tailwind utility classes"
      via: "@theme design token definitions"
      pattern: "--color-primary"
---

<objective>
Scaffold the Next.js 16 project with Tailwind CSS v4 design system, build the reusable layout primitives (section wrapper, navbar, WhatsApp FAB), and wire the page skeleton so all subsequent content sections can be dropped in.

Purpose: Establish the foundation that all other plans depend on -- project structure, design tokens, navigation, and primary conversion path (WhatsApp).
Output: A running Next.js app with sticky nav, mobile hamburger menu, smooth scroll, WhatsApp floating button, and placeholder sections ready for content.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-static-landing-page-whatsapp-conversion/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project with Tailwind v4 design system</name>
  <files>package.json, tsconfig.json, postcss.config.mjs, src/app/layout.tsx, src/app/globals.css, src/app/page.tsx, src/app/not-found.tsx, src/lib/constants.ts, src/components/ui/section-wrapper.tsx</files>
  <action>
1. Scaffold project using `pnpm create next-app@latest . --typescript --tailwind --app --src-dir --import-alias "@/*"` (run in project root since it already has git). If the scaffolder complains about existing files, use `--yes` or handle prompts.

2. Install Phase 1 dependencies: `pnpm add lucide-react sharp`

3. Replace `postcss.config.mjs` with Tailwind v4 PostCSS config:
```javascript
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
}
export default config
```

4. Replace `src/app/globals.css` with PACIFIQUE! design tokens using Tailwind v4 `@theme` directive. Include all colors from CLAUDE.md palette:
- primary: #4A4458, text-body: #595959 (darkened from #6B6B6B for WCAG contrast on lavender), bg-lavender: #F5F3F7, accent-deep: #4A3F5C, accent-deep-text: #FFFFFF, rose-light: #FDE8EC, rose-medium: #F9D5DC, crimson-dark: #7A2048, success: #22C55E, error: #EF4444, border-subtle: #E5E5E5, whatsapp: #25D366
- Font families: --font-heading (serif fallback), --font-body (sans-serif fallback), connected to next/font CSS variables
- Add `prefers-reduced-motion: reduce` media query to disable animations/transitions for accessibility

5. Create `src/app/layout.tsx`:
- Import Inter from `next/font/google` for body text (variable: `--font-body-family`, display: swap)
- Import a serif font for headings -- use Merriweather (weight: 400, 700, variable: `--font-heading-family`, display: swap). This is a placeholder per research open question #1, easily swappable via CSS variable.
- Set `lang="pt-BR"` on `<html>`
- Add `data-scroll-behavior="smooth"` attribute on `<html>` (Next.js 16 requirement for smooth scroll)
- Add `scroll-smooth` class on `<html>`
- Set metadata: title "PACIFIQUE! - Camara Privada de Conciliacao e Mediacao", description about the services with CNJ/TJPE credentials mention
- Body: `className="font-body text-text-body antialiased"`

6. Create `src/lib/constants.ts` with all shared data:
- `WHATSAPP_NUMBER = '5581987900892'` (digits only per research pitfall #5)
- `WHATSAPP_MESSAGE = 'Ola! Gostaria de saber mais sobre os servicos da PACIFIQUE!'`
- `NAV_ITEMS` array: objects with `{label, href}` for each section: Sobre (#sobre), Nucleos (#nucleos), Equipe (#equipe), Fontes Normativas (#fontes-normativas), Vantagens (#vantagens), Como Funciona (#como-funciona), Comparativo (#comparativo), FAQ (#faq), Contato (#contato)
- `SECTION_IDS` object mapping section names to their IDs
- Type definitions for `NavItem`, `NucleusItem`, `TeamMember`, `FaqItem`
- Data arrays for nuclei (7 items from CLAUDE.md), team (4 founders from CLAUDE.md), and FAQ (6-8 placeholder questions about mediacao/conciliacao -- content in pt-BR). Each nucleus item should include an icon name string (from lucide-react) to be resolved in the component.

7. Create `src/components/ui/section-wrapper.tsx`:
- Server component (no "use client")
- Props: `id: string`, `children: React.ReactNode`, `variant: 'white' | 'lavender'` (default 'white'), `className?: string`
- Renders `<section>` with id, py-16 md:py-24, background based on variant, inner div with max-w-7xl mx-auto px-4 sm:px-6 lg:px-8

8. Create `src/app/not-found.tsx`:
- Branded 404 page with PACIFIQUE! colors, link back to home

9. Create `src/app/page.tsx` with placeholder structure:
- Import Navbar, WhatsAppFab
- Render Navbar at top, main element with placeholder sections (just section IDs and heading text for now -- these will be replaced in Plans 02-04), Footer placeholder, WhatsAppFab
- Each placeholder section should use SectionWrapper with correct ID and alternating variant (white/lavender) so navigation already works

Ensure `pnpm dev` starts successfully and the page renders at localhost:3000.
  </action>
  <verify>
    <automated>cd /Users/icarosilva/Projects/pacifique && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>Next.js project scaffolded, dev server starts, page renders with design tokens applied (primary purple heading, text-body gray paragraph, lavender background visible on alternating sections), all Tailwind utility classes from @theme available.</done>
</task>

<task type="auto">
  <name>Task 2: Build sticky navbar with hamburger menu and WhatsApp floating button</name>
  <files>src/components/navbar.tsx, src/components/whatsapp-fab.tsx</files>
  <action>
1. Create `src/components/navbar.tsx` as a client component (`"use client"`):
- Import NAV_ITEMS from `@/lib/constants`
- Import `Menu`, `X` from `lucide-react`
- State: `activeSection` (string), `mobileMenuOpen` (boolean)
- Desktop: Fixed header at top (z-40), white/95 background with backdrop-blur-sm, border-b border-border-subtle. Logo text "PACIFIQUE!" in font-heading text-primary on the left. Horizontal nav links on the right, hidden on mobile (hidden lg:flex). Each link: text-sm text-text-body hover:text-primary transition-colors. Active link: text-primary font-semibold with bottom border indicator.
- Mobile: Hamburger button (lg:hidden) with Menu icon (44x44px touch target minimum per research). When open, replace with X icon. Mobile menu: full-width dropdown below header, white background, py-2. Each link: block px-4 py-3 text-text-body. Active: text-primary bg-bg-lavender. Tap link closes menu (setMobileMenuOpen(false)).
- Scroll spy: useEffect with IntersectionObserver, rootMargin '-80px 0px -70% 0px', threshold 0. Observe all sections by NAV_ITEMS href. On intersection, setActiveSection to entry.target.id. Cleanup: observer.disconnect().
- Clicking a nav link should let the browser handle smooth scroll via anchor href (no JavaScript scrollTo needed since we have scroll-smooth on html). Close mobile menu on link click.
- The header should have enough height (h-16 or similar) so main content doesn't get hidden behind it. Add corresponding pt-16 to the page body or main element.

2. Create `src/components/whatsapp-fab.tsx` as a server component (no state needed):
- Import WHATSAPP_NUMBER, WHATSAPP_MESSAGE from `@/lib/constants`
- Renders a fixed-position anchor tag: `bottom-6 right-6 z-50`
- Size: h-14 w-14, rounded-full, bg-whatsapp (uses the design token #25D366)
- Contains inline WhatsApp SVG icon (the standard WhatsApp logo path from the research doc), h-7 w-7 text-white
- Link: `https://wa.me/${WHATSAPP_NUMBER}?text=${encodeURIComponent(WHATSAPP_MESSAGE)}`
- target="_blank", rel="noopener noreferrer"
- aria-label="Abrir conversa no WhatsApp"
- Hover: scale-110 transition-transform
- Focus: focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-whatsapp
- Shadow: shadow-lg for elevation
- Bottom offset of 24px (bottom-6) provides clearance for iOS Safari bottom bar per research pitfall #2

3. Update `src/app/page.tsx` to import and render Navbar and WhatsAppFab in the correct positions (Navbar before main, WhatsAppFab after main/footer).
  </action>
  <verify>
    <automated>cd /Users/icarosilva/Projects/pacifique && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>Sticky navbar renders with PACIFIQUE! logo, desktop horizontal links, and mobile hamburger menu that toggles. Smooth scroll works when clicking nav links. Active section is highlighted. WhatsApp button floats at bottom-right with green background, clicking opens wa.me link. No horizontal overflow at 375px.</done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without errors
2. `pnpm dev` starts and page renders at localhost:3000
3. Navbar is sticky at top, nav links visible on desktop viewport
4. Hamburger menu opens/closes on mobile viewport (375px)
5. Clicking any nav link scrolls smoothly to the corresponding section
6. WhatsApp button is visible at bottom-right, link opens wa.me with correct number
7. No horizontal scrollbar at 375px viewport
8. Design tokens are applied: purple headings, gray body text, lavender backgrounds alternate
</verification>

<success_criteria>
- Next.js 16 project runs with Tailwind v4 design tokens matching CLAUDE.md palette
- Navbar: sticky, smooth scroll, scroll spy active section, hamburger on mobile, horizontal on desktop
- WhatsApp FAB: visible on all sections, correct wa.me link with pre-filled message
- Section wrapper creates alternating white/lavender backgrounds
- Page structure ready for content sections (Plans 02-04 drop in components)
- Build succeeds, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-static-landing-page-whatsapp-conversion/01-01-SUMMARY.md`
</output>
